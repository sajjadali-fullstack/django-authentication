Q. What is decorators?
>>> without changing the code of the paricular method / particular functionality on it its like a wrapper is called decorators.

                                                Steps 
                                                ``````

auth application use one table ==> user
Create superuser and go to admin interface then add users


Problem-1
---------
Page not found 
request Method : GET
Request URL : http://locolhost:8000/accounts/login/?next=/java/

solve this problem by including authentiacation application urls 

urls.py
-------
from django.urls import path, include
path('accounts/', include('django.contrib.auth.urls'))

Problem-2
---------
TemplateDoesNotExist at /accounts/login/
registeration/login.html 

=> Create a folder under templates with the name registration and create a file login.html


login.html 
-----------
Implement login functionality by injecting  {{form }} in login.html

Implemennt Logout functionality
----------------------------------

Step1-  Update logout link with the url patter: /accounts/logout/
        <a href="accounts/login/" class="nav-link">Login</a>


Step2 - Define seperate logout destination page:

logout.html 
-----------

views.py 
`````````
# Logout View 
def logout_view(request):
    logout(request)  # one user logout the session should be clear
    return render(request,'testapp/logout.html')

urls.py 
````````
path('logout/', views.logout_view, name='logout'),
  
<a href="{% url 'logout' %}" class="nav-link btn">Logout</a>    



Implementing signup button functionality
----------------------------------------

1. Auth application having form class to provide login form.
2. But auth application does not contain any form for signup functionality.
3. If a user signup, compulsory that information should be stored in DataBase(user table)
4. Display form to signup and that information should be stored inside DataBase directly.
5. For such type of requirement it is highly recommended to go for model based form.

forms.py
`````````

